/**
 * Shared constants used across the application
 */

/**
 * Port for the dev-browser HTTP server
 * Used by task-manager.ts and dev-browser skills
 */
export const DEV_BROWSER_PORT = 9224;

/**
 * Port for the dev-browser CDP (Chrome DevTools Protocol)
 * Used internally by dev-browser server and Playwright MCP CDP attachment
 */
export const DEV_BROWSER_CDP_PORT = 9225;
